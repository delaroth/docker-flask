pipeline {
    agent any

    stages {
        stage('echo') {
            steps {
                echo 'hi shahar'
            }

        }
        stage('docker build'){
             steps{
          sh 'docker build localhost:8081/delaroth/new-image:2'
         // sh 'docker push delaroth/new-image:2'
}
    }
}
}
