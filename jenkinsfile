pipeline {
    agent any

    stages {
        stage('echo') {
            steps {
                echo 'hi shahar'
            }

        }
        stage('docker build'){
             steps{
          sh 'docker tag new-image:2'
          sh 'docker build new-mage:2'
         // sh 'docker push new-image:2'
}
    }
}
}
