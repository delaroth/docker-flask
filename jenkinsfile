pipeline {
    agent any

    stages {
        stage('echo') {
            steps {
                echo 'hi shahar'
            }

        }
        stage('docker build'){
             steps{
          sh 'docker tag latest new-image'
          sh 'docker build new-image'
         // sh 'docker push new-image:2'
}
    }
}
}
