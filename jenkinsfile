pipeline {
    agent any

    stages {
        stage('echo') {
            steps {
                echo 'hi shahar'
            }

        }
        stage('docker build'){
         sh 'docker build delaroth/new-image:2'
         sh 'docker push delaroth/new-image:2'
    }
}
